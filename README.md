# K-Nearest_Neighbors
K-Nearest Neighbors using Python.

The steps to execute the code :<br />
•	First download both the K-Nearest Neighbors.py file and the dataset. And make shore both the files are in the folder.<br />							
•	Then open the python file in any of the Python compiler and run the code.
